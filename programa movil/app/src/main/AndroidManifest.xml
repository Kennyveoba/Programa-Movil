<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.crud_php">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CRUD_PHP">
        <activity
            android:name=".agregarMatricula"
            android:exported="false" />
        <activity
            android:name=".editar_cursos"
            android:exported="false" />
        <activity
            android:name=".AgregarCursos"
            android:exported="false" />
        <activity android:name=".AdmMatriculas" />
        <activity android:name=".AdmCursos" />
        <activity android:name=".detalles2" />
        <activity android:name=".detalles" />
        <activity android:name=".editar" />
        <activity android:name=".agregar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>